interface:
  display_name: "Investment Orchestrator"
  short_description: "Route requests through fixed investment pipelines"
  default_prompt: "Use $investment-orchestrator to route this request through the correct fixed investment pipeline."
