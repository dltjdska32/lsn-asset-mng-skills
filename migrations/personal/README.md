# Personal migrations

The executable ordered catalog lives at `runtime/investment_stack/migrations/personal`. It is packaged with the runtime and must remain independent from the run database catalog.

The current catalog adds append-only ledger and rebuildable projection support in version 3.
