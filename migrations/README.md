# Migration boundary

Personal and per-run schema histories are independent. Executable migrations are packaged under `runtime/investment_stack/migrations` so source and installed behavior use the same version-controlled catalog.
